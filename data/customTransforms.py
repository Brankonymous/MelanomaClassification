class LabelToBinary(object):
    def __call__(self, sample):
        if sample == 'malignant':
            return 1
        else:
            return 0

class LabelToBinary(object):
    def __call__(self, sample):
        if sample == 'malignant':
            return 1
        else:
            return 0